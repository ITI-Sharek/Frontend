# Sharek backend origin. The NestJS API listens on 4000 by default.
VITE_API_URL=http://localhost:4000

# Optional: opt into the mocked social-auth flow.
# See src/modules/auth/services/social-auth.service.ts
# VITE_SOCIAL_AUTH_DEMO=1
