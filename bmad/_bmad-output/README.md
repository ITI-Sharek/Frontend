# BMAD Output Moved

Shared BMAD product output is canonical in the sibling `docs` repository.

New raw BMAD output is configured to land in `../../../docs/generated/bmad/`.
Review and promote it deliberately into the canonical `product/`,
`architecture/`, or `design/` area. Do not restore shared product documents to
this directory.

