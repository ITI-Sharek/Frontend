# Frontend Implementation Design

This directory now contains only frontend-specific implementation planning and
the local design-system assets.

Canonical product experience, state model, journeys, screen inventory, and
wireframes live in the sibling documentation repository at `../../../docs/`.

