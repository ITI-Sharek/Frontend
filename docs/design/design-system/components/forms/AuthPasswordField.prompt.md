Password input with a lock glyph and a show/hide eye toggle (self-contained visibility state).

```jsx
<AuthPasswordField label="كلمة المرور" autoComplete="current-password" />
```

Always LTR internally regardless of surrounding RTL layout (passwords are typed LTR).
