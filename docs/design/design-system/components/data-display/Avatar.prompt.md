Circular profile picture, three sizes, optional online-status dot — from the Figma "PP/light" (Size × State variants) family.

```jsx
<Avatar src="/assets/logo-mark.png" size="md" online />
```

Falls back to a tinted placeholder circle when `src` is omitted.
