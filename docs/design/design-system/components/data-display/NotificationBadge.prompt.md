Icon button with a small red unread-count badge pinned to the top-right corner — from the Figma "Notification_Badge" component family (Type/Color variants collapsed into one `count`-driven component).

```jsx
<NotificationBadge icon="bell" count={2} />
```

Omit/zero `count` to render a plain icon with no badge.
