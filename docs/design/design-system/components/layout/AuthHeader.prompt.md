Top bar for auth-flow pages: "Sharek" wordmark (Geist, teal, LTR) on one side, dark/light toggle + Arabic language switch on the other.

```jsx
<AuthHeader />
```

Self-contained demo theme toggle — wire to your real theme context in production.
