/** Consent version the backend currently accepts for GitHub skill analysis. */
export const SKILL_ANALYSIS_CONSENT_VERSION = "github-skill-analysis-v1";

/** Backend rule: at least 1 and at most 10 repositories per generation. */
export const MIN_ANALYSIS_REPOSITORIES = 1;
export const MAX_ANALYSIS_REPOSITORIES = 10;
